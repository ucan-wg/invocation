digraph {
    rankdir="LR"

    node [
      style=rounded
      shape=box
    ]

    Invocation -> Pointer -> Receipt -> Promise -> Pipeline
    Pointer -> Receipt -> Memoization
    Invocation -> Batch -> Pipeline
}
