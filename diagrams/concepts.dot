digraph {
    node [
      style = rounded
      shape = box
    ]

    Closure -> Task  -> Invocation
    Closure -> Batch -> Invocation -> Receipt -> Memoization
                                      Receipt -> Promise -> Pipeline
                                                 Batch   -> Pipeline
}
