digraph {
    node [
      style = rounded
      shape = box
    ]

    Task -> Metadata -> Invocation
    Task -> Batch    -> Invocation -> Receipt -> Memoization
                                      Receipt -> Promise
}
