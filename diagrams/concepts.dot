digraph {
    node [
      style = rounded
      shape = box
    ]

    Closure -> Metadata -> Invocation
    Closure -> Batch    -> Invocation -> Receipt -> Memoization
                                         Receipt -> Promise
}
