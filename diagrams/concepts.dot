digraph {
    node [
      style = rounded
      shape = box
    ]

    Invocation -> Metadata -> Envelope
                  Batch    -> Envelope -> Receipt -> Memoization
                  Receipt  -> Promise
    Invocation -> Batch
}
