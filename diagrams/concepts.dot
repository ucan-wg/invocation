digraph {
    rankdir="LR"

    node [
      style=rounded
      shape=box
    ]

    Invocation -> Pointer -> Memoization
    Invocation -> Batch -> Pipeline
    Invocation -> Receipt -> Promise -> Pipeline
}
